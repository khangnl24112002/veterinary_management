import db from "../models/index";

const getAll = () => {};

const fingById = (id) => {};

const insert = (drug) => {};

const update = (id, drug) => {};

const delDrug = (id) => {};

module.exports = {
  getAll,
  fingById,
  insert,
  update,
  delDrug,
};
